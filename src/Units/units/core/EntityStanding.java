package units.core;

import zap.turrem.core.entity.Entity;
import zap.turrem.core.entity.article.EntityArticle;

public class EntityStanding extends Entity
{
	public EntityStanding(EntityArticle article)
	{
		super(article);
	}
}
