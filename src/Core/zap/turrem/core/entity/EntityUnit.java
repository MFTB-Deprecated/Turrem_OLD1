package zap.turrem.core.entity;

public abstract class EntityUnit extends EntitySoft implements IEntityUnit
{
	public boolean selected = false;

	public void mouseEvent(boolean me)
	{
		super.mouseEvent(me);
	}
}
